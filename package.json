{
  "name": "noted",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "docker:up": "docker-compose up -d || docker-compose.exe up -d",
    "docker:down": "docker-compose down || docker-compose.exe down",
    "docker:build": "docker-compose build || docker-compose.exe build",
    "docker:restart": "docker-compose restart || docker-compose.exe restart"
  },
  "dependencies": {
    "drizzle-orm": "^0.40.0",
    "next": "15.1.7",
    "postgres": "^3.4.5",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "drizzle-kit": "^0.30.5",
    "eslint": "^9",
    "eslint-config-next": "15.1.7",
    "typescript": "^5"
  }
}
